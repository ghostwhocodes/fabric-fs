version: "3.8"

services:
  nats:
    image: nats:latest
    container_name: fabricfs-nats
    restart: unless-stopped
    ports:
      - "4222:4222" # NATS client
      - "8222:8222" # HTTP monitoring
    command: >
      -js
      -m 8222
      --name fabricfs-nats

